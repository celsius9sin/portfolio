# portfolio

エンジニアとしての経験・スキルをまとめたポートフォリオサイト。

- `index.html` / `style.css` / `app.js`: 静的サイト本体(手動編集可、ビルド不要)。`data.json` を読み込んで描画する。
- `data.json`: 掲載内容。**自動生成・上書きされるため手動編集しない。**

公開URL: GitHub Pages(このリポジトリの main ブランチ、ルートから配信)。

## GitHubユーザー名の変更(2026-09-21)

GitHubのユーザー名を `nasgamer` から `celsius9sin` に変更した(GitHubのユーザーIDは変わらない)。このリポジトリのURLは `https://github.com/celsius9sin/portfolio` になった。文書やスクリプトに `nasgamer` が残っていたら、旧ユーザー名として読み替えること。旧URLは当面リダイレクトされるが、remote URL は新URLに更新する。gitのnoreplyメールアドレスは `206295932+celsius9sin@users.noreply.github.com`。
